{
  "version": 2,
  "builds": [
    {
      "src": "wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "maxDuration": 300,
        "memory": 1024
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "wsgi.py" }
  ]
}